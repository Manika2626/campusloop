import UploadForm from './UploadForm';

export const metadata = {
  title: 'Upload Announcement - Admin Page',
  description: 'Upload your club or teacher announcements easily.',
};

export default function Page() {
  return <UploadForm />;
}
