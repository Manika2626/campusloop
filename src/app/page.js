export const metadata = {
  title: "Campus Loop | Login",
  description: "Login to Campus Loop with your official college email to view campus announcements and updates.",
};

import HomeClient from "./HomeClient";

export default function Page() {
  return <HomeClient />;
}
